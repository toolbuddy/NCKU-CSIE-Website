extends /layouts/research.pug

block append stylesheets

block append content
  - const i18n = require( path.resolve( languageRoot, 'research/awards' ) );

block append scripts
  +js( 'research/awards' )
