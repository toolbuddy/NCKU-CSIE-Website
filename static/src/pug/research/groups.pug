extends /layouts/research.pug

append data
  -
    const i18n = Object.freeze( {
      'en-US': {
        'title': 'Research Groups',
      },
      'zh-TW': {
        'title': '研究群',
      },
    } );
    const currentRoute = 'research/groups';

append title
  +title( i18n[ language ].title )

append stylesheets
  +css( 'research/groups' )

append header

append content

append footer

append scripts
  +js( 'research/groups' )
