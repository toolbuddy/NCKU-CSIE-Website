mixin button( belongBlock, type ) 
  button( class=`block__button block__button--${ type }` id=`${ belongBlock }__${ type }` )
    +image( {
        alt:       '>',
        className: `block__button--img`,
        src:       'icon/arrow-right.png',
    } )