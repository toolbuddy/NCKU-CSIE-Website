mixin technology-transfer-card( data )
  -
    const i18n =  Object.freeze({
      [ LANG.getLanguageId( 'en-US' ) ]: {
        topic: {
          duration:         'duration',
          authorizingParty: 'authorizing party',
          authorizedParty:  'authorized party',
          technology:       'technology',
        },
      },
      [ LANG.getLanguageId( 'zh-TW' ) ]: {
        topic: {
          duration:         '合約期間',
          authorizingParty: '授權方名稱',
          authorizedParty:  '接受方名稱',
          technology:       '技術名稱',
        }
      },
    })
  section.technology-transfer-card.cards__technology-transfer-card( id=`cards__technology-transfer-card--${ data.technologyTransferId }` )
    section.technology-transfer-card__content
      article.content__element.content__element--duration
        h4.element__topic.topic= i18n[ LANG.id ].topic.duration
        p.element__value.value= `${data.from} - ${data.to}`
      article.content__element.content__element--technology
        h4.element__topic.topic= i18n[ LANG.id ].topic.technology
        p.element__value.value= data.technology
      article.content__element.content__element--authorizing-party
        h4.element__topic.topic= i18n[ LANG.id ].topic.authorizingParty
        p.element__value.value= data.authorizingParty
      article.content__element.content__element--authorized-party
        h4.element__topic.topic= i18n[ LANG.id ].topic.authorizedParty
        p.element__value.value= data.authorizedParty

    img.technology-transfer-card__patch(
      alt = 'patch',
      id = `technology-transfer-card__patch--${ data.technologyTransferId }`,
      src = UTILS.staticUrl( { href: 'image/icon/edit.png' } ),
      data-id = data.technologyTransferId
    )
    img.technology-transfer-card__delete(
      alt = 'delete',
      id = `technology-transfer-card__delete--${ data.technologyTransferId }`,
      src = UTILS.staticUrl( { href: 'image/icon/delete.png' } )
      data-id = data.technologyTransferId
    )