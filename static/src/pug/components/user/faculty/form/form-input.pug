include /components/user/faculty/form/text.pug
include /components/user/faculty/form/i18n-text.pug

mixin form-input( modifier, method, formI18n, inputConfig )
  - 
    const i18n = Object.freeze( {
      [ LANG.getLanguageId( 'en-US' ) ]: {
        button: {
          check: 'check',
          cancel: 'cancel',
        },
      },
      [ LANG.getLanguageId( 'zh-TW' ) ]: {
        button: {
          check: '確定',
          cancel: '取消',
        }
      }
    })
  form.form-input.form__form-input(id=`form-${modifier}-${method}`)
    h2.form-input__title.title= formI18n[ LANG.id ][ method ]
    section.form-input__content.content
      if method === 'put' || method === 'post'
        each config in inputConfig
          if config.type === 'i18n-text'
            +i18n-text( config, formI18n )
          if config.type === 'text'
            +text( config, formI18n )
        p.content__error-message.error-message= '獎項不可為空'
    section.form-input__button.button
      button.button__check.check= i18n[ LANG.id ].button.check
      button.button__cancel.cancel= i18n[ LANG.id ].button.cancel
    
    