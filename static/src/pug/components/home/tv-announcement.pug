include /components/common/image.pug

figure.series__show.show.show--hide
  section.show__ad
    img(
      alt = data.title,
      class = 'ad__image',
      src = `data:image/jpeg;base64,${ data.image }`,
    )
    header.ad__title= data.title
