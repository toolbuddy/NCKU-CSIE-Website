extends /layouts/base.pug

append data
  -
    const i18n = Object.freeze( {
      [ LANG.getLanguageId( 'en-US' ) ]: {
        title:　'Laboratories',
        topic:  'All Laboratories',
        breadcrumb: {
          home:     'Home',
          about: 'About',
        },
      },
      [ LANG.getLanguageId( 'zh-TW' ) ]: {
        title:　'實驗室',
        topic:  '實驗室列表',
        breadcrumb: {
          home:     '首頁',
          about: '關於本系',
        },
      },
    } );
    const currentRoute = 'about/lab';

append title
  +title( i18n[ LANG.id ].title )

append stylesheets
  +css( 'about/lab' )

append header
  +banner(
    'body',
    i18n[ LANG.id ].title
  )

append breadcrumb
  +breadcrumb(
    'content',
    [
      { text: i18n[ LANG.id ].breadcrumb.home, route: '' },
      { text: i18n[ LANG.id ].breadcrumb.about, route: 'about' },
      { text: i18n[ LANG.id ].title },
    ]
  )

append content
  include /components/common/topic.pug
  include /components/common/loading.pug
  include /components/common/no-result.pug

  +topic( 'content', i18n[ LANG.id ].topic )

  section#lab.content__lab.lab
    +noResultWithImage( 'lab' )
    +loading( 'lab' )
    section.lab__cards.cards

append footer

append scripts
  +js( 'about/lab' )
