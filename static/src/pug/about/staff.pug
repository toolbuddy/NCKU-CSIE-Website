extends /layouts/about.pug

append data
  -
    const i18n= Object.freeze( {
      'en-US': {
        'title': 'Staff',
      },
      'zh-TW': {
        'title': '行政人員',
      },
    } );
    const currentRoute = 'about/staff';

append title
  +title( i18n[ language ].title )

append stylesheets
  +css( 'about/staff' )

append header

append content
  include /components/common/not-available.pug
  +notAvailable()

append footer

append scripts
  +js( 'about/staff' )
