extends /layouts/about.pug

append data
  -
    const i18n = Object.freeze( {
      'en-US': {
        'title': 'About Us',
      },
      'zh-TW': {
        'title': '關於本系',
      },
    } );

append title
  +title( i18n[ language ].title )

append stylesheets
  +css( 'about/index' )

append header

append content

append footer

append scripts
  +js( 'about/index' )
