extends /layouts/resource.pug

append data
  -
    const i18n = Object.freeze( {
      'en-US': {
        'title': 'Resource',
      },
      'zh-TW': {
        'title': '資源連結',
      },
    } );

append title
  +title( i18n[ language ].title )

append stylesheets
  +css( 'resource/index' )

append header

append content

append footer

append scripts
  +js( 'resource/index' )
