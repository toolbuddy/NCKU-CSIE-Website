extends /layouts/resource.pug

block append stylesheets

block append content
  - const i18n = require( path.resolve( languageRoot, 'resource/rule' ) );

block append scripts
  +js( 'resource/rule' )
