extends /layouts/student.pug

block append stylesheets

block append content
  - const i18n = require( path.resolve( languageRoot, 'student/international' ) );

block append scripts
  +js( 'resource/international' )
