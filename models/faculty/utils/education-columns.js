import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/education-columns.js';

export const educationColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default educationColumnsUtils;
