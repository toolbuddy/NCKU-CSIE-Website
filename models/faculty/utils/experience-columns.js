import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/experience-columns.js';

export const experienceColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default experienceColumnsUtils;
