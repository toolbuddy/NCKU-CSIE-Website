import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/project-columns.js';

export const projectColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default projectColumnsUtils;
