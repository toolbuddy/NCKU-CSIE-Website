import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/title-columns.js';

export const titleColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default titleColumnsUtils;
