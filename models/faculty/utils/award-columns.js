import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/award-columns.js';

export const awardColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default awardColumnsUtils;
