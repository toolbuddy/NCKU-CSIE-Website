import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/publication-columns.js';

export const publicationColumnsUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default publicationColumnsUtils;
