import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/project-category.js';

export const projectCategoryUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default projectCategoryUtils;
