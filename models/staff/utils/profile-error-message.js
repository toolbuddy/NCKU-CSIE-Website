import I18nUtils from '../../common/utils/i18n.js';
import { defaultOption, i18n, map, } from '../maps/profile-error-message';

export const profileErrorMessageUtils = new I18nUtils( {
    defaultOption,
    i18n,
    map,
} );

export default profileErrorMessageUtils;
